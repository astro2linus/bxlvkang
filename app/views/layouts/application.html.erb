<!DOCTYPE html>
<%= html_tag :class => "no-js" %>
  <%= render "head" %>

  <body class="<%= body_class %>">
    <%= render "chromeframe" %>
    <%= render "flashes" %>

    <div class="container">
      <%= render "header" %>
      <%= yield %>
      <%= render "footer" %>
    </div>

    <%# Javascript at the bottom for fast page loading %>
    <%= render "javascripts" %>
  </body>
</html>
